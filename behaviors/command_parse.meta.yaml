name: command_parse
description: Parse a natural-language task into executable steps.
inputs: [task]
outputs: [steps]
effects: [have_steps]
cost: 1.0
capabilities: [analytic]
requires_explanation: true
